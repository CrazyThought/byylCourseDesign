1. **修复状态转移合并错误**：

   * 修改displayNFA和displayDFA函数，确保每个转移条件列仅显示唯一对应的目标状态值

   * 对于DFA，每个状态和转移应该只有一个目标状态，只返回第一个找到的目标状态

   * 对于NFA，确保返回的目标状态是唯一的集合

2. **修复字符替换与列处理不当问题**：

   * 修改mergeTransitionsByRef函数，确保只保留引用名称和没有被引用的字符

   * 删除原有的字符列，只显示引用名称列

3. **修复特殊字符集显示缺失问题**：

   * 检查并完善processRegexReferences函数，确保它正确处理所有类型的字符集

   * 确保所有定义的字符集（包括特殊字符集）均正确显示在状态转移图中

4. **测试修复效果**：

   * 确保修复后符合需求规范

   * 确保所有定义的字符集都能正确显示

   * 确保没有多余的字符列

   * 确保每个转移条件列仅显示唯一对应的目标状态值

