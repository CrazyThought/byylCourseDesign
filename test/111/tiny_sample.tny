{ 这是一个Tiny语言样例程序，包含所有Tiny语言的单词和语句类型 }

{ 变量声明和赋值 }
x := 10
y := 20
z := 0

{ if语句示例 }
if x < y then
    z := x + y
end

{ if-else语句示例 }
if x > y then
    z := x - y
else
    z := y - x
end

{ repeat语句示例 }
repeat
    x := x + 1
    y := y - 1
until x >= y

{ 输入语句示例 }
read input_var

{ 输出语句示例 }
write x
write y
write z

{ 复杂表达式示例 }
result := (x + y) * (z - 5) / 2 % 3 ^ 2

{ 比较运算符示例 }
if x <> y then
    write "x不等于y"
end

if x <= y then
    write "x小于等于y"
end

if x >= y then
    write "x大于等于y"
end

{ 算术运算符示例 }
a := 15 + 3
b := 20 - 5
c := 4 * 6
d := 30 / 2
e := 17 % 5
f := 2 ^ 3

{ 语句序列示例 }
var1 := 100; var2 := 200; var3 := var1 + var2

{ 嵌套表达式示例 }
complex_result := ((a + b) * c - d) / e ^ f

{ 最终输出 }
write "程序执行完毕"
write result
write complex_result